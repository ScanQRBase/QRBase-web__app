// Encrypts a JavaScript object into a base64 string using a secret key.
export function encryptObject(obj: any, secret: string) {
   // Encrypts a JavaScript object into a base64 string using a secret key.
  }
  
  
  export function decryptObject(encryptedData: any) {
   // Decrypts an encrypted string (that was previously encrypted using `encryptObject`)
  }
  
 
  export const decryptObjectFullImage = async (encryptedData: string): Promise<string[]> => {
     // Decrypts a encoded string containing an object with an 'images' array field

  };